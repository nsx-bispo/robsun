import { chromium } from 'playwright'
import { spawn } from 'node:child_process'
import fs from 'node:fs/promises'

const host='127.0.0.1',port=4173,baseUrl=`http://${host}:${port}/robsun/`,resultsDir='test-results'
await fs.mkdir(resultsDir,{recursive:true})
const server=spawn('npm',['run','preview','--','--host',host,'--port',String(port)],{stdio:['ignore','pipe','pipe'],shell:process.platform==='win32'})
let serverOutput='';server.stdout.on('data',c=>serverOutput+=c.toString());server.stderr.on('data',c=>serverOutput+=c.toString())
async function waitForServer(){for(let i=0;i<60;i+=1){try{const r=await fetch(baseUrl);if(r.ok)return}catch{}await new Promise(r=>setTimeout(r,250))}throw new Error(`Vite preview did not start.\n${serverOutput}`)}
function assert(condition,message){if(!condition)throw new Error(message)}
async function settle(page){await page.waitForTimeout(360)}
async function assertNoHorizontalOverflow(page,label){const m=await page.evaluate(()=>({innerWidth:window.innerWidth,scrollWidth:document.documentElement.scrollWidth,bodyScrollWidth:document.body.scrollWidth}));assert(m.scrollWidth<=m.innerWidth+1,`${label}: document overflow ${JSON.stringify(m)}`);assert(m.bodyScrollWidth<=m.innerWidth+1,`${label}: body overflow ${JSON.stringify(m)}`)}
async function assertTapTargets(page,label){const bad=await page.locator('button:visible, a.btn:visible, input:visible, select:visible, summary:visible').evaluateAll(elements=>elements.map(el=>{const r=el.getBoundingClientRect();return{tag:el.tagName,text:(el.textContent||el.getAttribute('aria-label')||'').trim().slice(0,80),height:r.height}}).filter(x=>x.height>0&&x.height<43));assert(bad.length===0,`${label}: undersized tap targets ${JSON.stringify(bad)}`)}
async function testViewport(browser,viewport,label,mobile){const context=await browser.newContext({viewport,deviceScaleFactor:1,reducedMotion:'no-preference'});const page=await context.newPage();page.setDefaultTimeout(8000);page.setDefaultNavigationTimeout(10000);const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});page.on('pageerror',e=>errors.push(e.message));try{await page.goto(baseUrl,{waitUntil:'domcontentloaded'});await page.locator('#root').waitFor({state:'attached'});await settle(page);await assertNoHorizontalOverflow(page,label);await assertTapTargets(page,label);const simulatorTop=await page.locator('#simulador').evaluate(el=>el.getBoundingClientRect().top+window.scrollY),includedTop=await page.locator('#incluido').evaluate(el=>el.getBoundingClientRect().top+window.scrollY);assert(simulatorTop<includedTop,`${label}: calculator moved too far down`);const first=(await page.locator('#typewriter').textContent())?.trim()||'';assert(first.length>0,`${label}: typewriter started empty`);await page.waitForTimeout(1850);const second=(await page.locator('#typewriter').textContent())?.trim()||'';assert(second.length>0,`${label}: typewriter entered empty state`);await page.locator('#simulador').scrollIntoViewIfNeeded();await settle(page);if(mobile){assert(await page.locator('.mobile-calculator-scene:visible').first().isVisible(),`${label}: integrated mobile scene missing`);assert(await page.locator('.desktop-preview:visible').count()===0,`${label}: desktop preview occupies mobile`);const card=await page.locator('.wizard-card').boundingBox(),scene=await page.locator('.solar-scene-compact:visible').first().boundingBox();assert(card&&scene,`${label}: card or scene missing`);assert(scene.x>=card.x-1,`${label}: scene escapes left`);assert(scene.x+scene.width<=card.x+card.width+1,`${label}: scene escapes right`);assert(await page.locator('.result-card-mobile').isVisible(),`${label}: mobile results missing`);await page.getByRole('button',{name:/Continuar/}).click();await settle(page);assert(await page.locator('#cityCep').isVisible(),`${label}: city/CEP missing`);assert(await page.locator('#roofType').isVisible(),`${label}: roof type missing`);assert(await page.locator('.mobile-calculator-scene:visible').count()===1,`${label}: transition did not settle`);await page.getByRole('button',{name:/Continuar/}).click();await settle(page);assert(await page.locator('#orientationGroup').isVisible(),`${label}: orientation controls missing`);await page.getByRole('button',{name:'Sul',exact:true}).click();await page.waitForTimeout(260);assert(await page.locator('.solar-scene-compact:visible').first().getAttribute('data-orientation')==='S',`${label}: scene did not react to orientation`);await page.getByRole('button',{name:/Continuar/}).click();await settle(page);const details=page.locator('.advanced-details');assert(await details.isVisible(),`${label}: advanced details missing`);await details.locator('summary').click();assert(await page.locator('#tilt').isVisible(),`${label}: tilt missing`);assert(await page.locator('#gdRule').isVisible(),`${label}: GD rule missing`);assert(await page.locator('#connection').isVisible(),`${label}: connection type missing`);await assertNoHorizontalOverflow(page,`${label} after wizard`)}else{assert(await page.locator('.desktop-preview').isVisible(),`${label}: desktop preview missing`);assert(await page.locator('.desktop-preview .solar-scene').isVisible(),`${label}: desktop scene missing`);const wizard=await page.locator('.wizard-card').boundingBox(),preview=await page.locator('.desktop-preview').boundingBox();assert(wizard&&preview&&preview.x>wizard.x,`${label}: desktop two-column layout broken`)}await page.screenshot({path:`${resultsDir}/${label}.png`,fullPage:true,animations:'disabled'});assert(errors.length===0,`${label}: browser errors ${errors.join(' | ')}`)}catch(error){await page.screenshot({path:`${resultsDir}/${label}-failure.png`,fullPage:true,animations:'disabled'}).catch(()=>{});throw error}finally{await context.close()}}
let browser
try{await waitForServer();browser=await chromium.launch({headless:true});await testViewport(browser,{width:320,height:700},'mobile-320',true);await testViewport(browser,{width:393,height:852},'iphone-393',true);await testViewport(browser,{width:430,height:932},'mobile-430',true);await testViewport(browser,{width:1440,height:1000},'desktop-1440',false);console.log('✓ Browser E2E checks passed for 320, 393, 430 and desktop viewports.')}finally{if(browser)await browser.close();server.kill('SIGTERM')}
